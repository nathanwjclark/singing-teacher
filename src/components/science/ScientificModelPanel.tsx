import {useState} from 'react';
import {scienceAsset,useScienceStatus} from './scienceClient';
import './ScientificModel.css';
export function ScientificModelPanel({onPreview}:{onPreview:()=>void}){
 const state=useScienceStatus(),r=state.result;const [notice,setNotice]=useState(''),[starting,setStarting]=useState(false);
 async function run(){setStarting(true);setNotice('');try{const response=await fetch('/api/science/run',{method:'POST'});const data=await response.json();if(!response.ok)throw Error(data.error);setNotice('Native search, prospective forecasts and geometry export started. All attempts are retained.')}catch(e){setNotice(String(e))}finally{setStarting(false)}}
 return <section className="scientific-model"><h3>Scientific model</h3><p>Local voice fitting → frozen acoustic predictions → native vocal-tract geometry.</p><div className="scientific-actions"><button onClick={()=>void run()} disabled={starting||state.status==='running'||state.status==='unavailable'}>{state.status==='running'?'Model jobs running…':'Run model on saved voice capture'}</button>{r&&<button onClick={onPreview}>Preview predicted geometry</button>}<span>{state.status}</span></div><p role="status">{notice||state.error}</p>{r&&<><p><strong>Actual native run:</strong> {r.nativeCalls} calls · {r.calibrationWindows} voice windows · {r.jobs.map(j=>j.operation).join(' → ')}. Source capture {r.sourceCaptureId?.slice(0,8)||'retained in import receipt'}.</p><p>Candidate discrepancy {r.fitDiscrepancy.toFixed(3)}; fixed-anatomy comparison {r.baselineDiscrepancy.toFixed(3)}. Lower discrepancy here does not establish correct anatomy.</p><table><thead><tr><th>Predicted vowel</th><th>Hypothesis</th><th>Pitch</th><th>Brightness (centroid)</th><th>Loudness</th></tr></thead><tbody>{r.forecast.rankings.flatMap(rank=>rank.predictions.map((p,i)=>{const m=p.canonical?.measurement.measurements??[];const value=(name:string)=>{const x=m.find(v=>v.name===name);return x?.value==null?'Unavailable':`${x.value.toFixed(1)} ${x.unit}`};return <tr key={`${rank.experiment.pose}-${i}`}><td>{rank.experiment.pose}</td><td>{p.hypothesis_id}</td><td>{value('pitchHz')}</td><td>{value('centroidHz')}</td><td>{value('dbfs')}</td></tr>}))}</tbody></table><p>{r.forecastRole} Conditions: jaw −3°, source pitch 180 Hz, digital gain 4. {r.forecast.selected_experiment_id?`Largest useful candidate separation: ${r.forecast.selected_experiment_id}.`:'No separating next experiment found.'}</p><details><summary>Geometry parameters and retained evidence</summary><table><thead><tr><th>Parameter</th><th>Candidate</th><th>Reference</th></tr></thead><tbody>{Object.entries(r.anatomy).map(([k,v])=><tr key={k}><td>{k}</td><td>{v.toFixed(3)}</td><td>{r.referenceAnatomy[k]?.toFixed(3)}</td></tr>)}</tbody></table><p>{r.interpretation} These parameters describe the vocal-tract model; they do not measure shoulder muscles, muscle tension or hidden tissue.</p><a href={scienceAsset(r.runId,'fit.json')} target="_blank" rel="noreferrer">Full fit</a> · <a href={scienceAsset(r.runId,'forecast.json')} target="_blank" rel="noreferrer">Frozen forecasts</a></details></>}</section>
}
