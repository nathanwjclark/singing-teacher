# Lead A interruption recovery and revision 8 handoff

Final verification: 286 Python tests with `-W error`, 8 KIT integration tests,
5 probe bridge tests, strict scientific TypeScript checks, app build/lint,
24 focused B tests, 6 coaching tests and 4 browser tests pass. The fresh active
replay used 24 calls and preserved its 2.45 mm anatomical error. Build bundle-size
advisory remains; actual human/live Astra demonstration is not claimed.

Publication receipt: PR #3 was already merged at its older head. This recovered
queue therefore requires a follow-up PR. Final B delta through `5e919bb` adds
sequential video/sound capture, raw depth feedback and a native model readiness
report. It preserves separate acquisition phases and explicitly abstains where
tongue correspondence is missing. The merged dynamic-handoff tests (11), app build
and lint passed again. Local iOS rebuild is blocked: this host has only Command
Line Tools selected and no `/Applications/Xcode*.app`; B's unsigned-build report
is retained as producer evidence, not a locally repeated check.

The usage interruption preserved all worktrees. This ledger reconciles agent
messages with actual files and commits; an agent stopping does not mark its lane
complete. Revision 8's research-harness finish plan governs delivery. Negative
scientific outcomes do not block a correctly connected implementation.

| Lane | Recovered evidence | Integration state |
|---|---|---|
| PCM search | `ddbaed1` | Integrated; actual service replay and KIT export tested |
| PCM active design/update | `130e09e` | Integrated; service-backed 24-call loop executed |
| PCM robustness | `c8aef83`, `43cebee` | Integrated; all six frozen cases and negative findings retained |
| Independent depth targets | `cf13ed6` | Integrated; analytic projection/target tests passed |
| Visible tongue handoff | `37f7eaa` | Integrated; prevented tongue-driven alignment leakage |
| Dynamic tongue handoff | `5ce0db6` | Recovered after agent stopped; integrated and targeted tests passed |
| External-drive physics | `e8d93bd` | Integrated; analytical and native-geometry checks passed |
| Probe forecast | `dfd56c1` | Integrated; actual spawned immutable forecast checked |
| Probe inverse | `8860edf`, `b223dfa` | Integrated; calibration/source validation fixes recovered |
| Probe ingestion bridge | Recovered as `ab54dbb`, `afdf931` | Integrated; B raw fixture → exact response → actual joint fit passes; private modes and capture binding verified |
| Scientific review | Recovered as `e6b98cb`, final `5cf774c` | Integrated; core and raw bridge reviewed, identified defects fixed and independently checked |
| Root service/CLI | Saved edits and `test_probe_service.py` | Committed as `84c34fc`; actual joint-fit CLI tested |

No old worktree was deleted or reset. Superseded work remains available for audit.
The original creative-ideas agent is unrelated to the current technical project
and has no unfinished implementation responsibility.

## Remote and plan receipt

Main through `703492f` supplies native probe acquisition, exact-drive/raw-response
import, repeated response extraction, acoustic mapping review UI and dynamic
tongue review. Plan commit `ab3e737` adds revision 8 and is merged into A's queue.
A consumes B's original media and full response artifacts; the browser summary
alone cannot certify a fit. No alternate public probe contract is introduced.

## Finish responsibilities

A owns the verified-media bridge, numerical operators, fit/forecast jobs and
reproducible service commands. B owns runtime Astra invocation/tool routing,
session/UI integration, actual device operation and independent demonstration.
The human capture owner supplies original private recordings and supported
instrument/placement evidence. None of those recordings is invented or uploaded
to public git by A.

The finish check distinguishes implementation, evidence source and scientific
outcome. Current numerical integration evidence is synthetic; physical/human
execution remains to be exercised on supplied media. Poor reconstruction scores
are retained results, not a reason to disconnect the working workflow. Additional
optimizer tuning and broad accuracy studies are deferred under revision 8.

## Concrete B consumption path

1. Run `science/scripts/import_probe_science.ts` against the original native probe
   directory and source-bound supplemental configuration, as documented in
   `science/PROBE_IMPORT.md`. Retain its eligibility receipt and B's immutable
   acquisition record; these are not themselves a fit result.
2. Put the emitted probe document in `parameters.probe_observations` and ordinary
   singing observations in `parameters.observations` of a `fit_probe_pcm` request.
   Declare finite shared anatomy and per-trial controls as in `PROBE_INVERSE.md`.
3. Run `python -m singing_physics.cli job REQUEST --output NEW_PRIVATE_DIRECTORY`.
   The result records actual contribution/rejection, candidates, counts and model
   assumptions. A successful CLI run verifies its stored manifest.
4. Use `predict_probe` with a frozen full-geometry snapshot before collecting its
   target. Runtime Astra and B's session ledger must consume these numerical
   results and retain decision/attempt/score/model lineage. They must not treat a
   browser-imported summary as a verified raw-media fit request.

The original known-filter software capture traversed ingestion and joint fitting
with 12 operator calls and large discrepancy (~6126), correctly demonstrating a
connected path with a poor physical-model match. Actual human probe recording
and runtime Astra orchestration have not been executed by this A workstream.
